# partially retroactive priority queue

class rpq:
	def __init__(self, q_now, max_or_min)
		self.q_now = q_now
		self.max_or_min = max_or_min


	def insert_insert(x, t):

		return

	def insert_delete(t):
		return

	def delete_insert(operation):
		return

	def delete_delete(operation):
		return
